
import React, { useState, useContext } from 'react';
import { useNavigate } from 'react-router-dom';
import { AuthContext } from '../context/AuthContext';
export default function Register(){ const { register } = useContext(AuthContext); const [name,setName]=useState(''); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const nav=useNavigate(); const handle=async(e)=>{ e.preventDefault(); try{ await register(name,email,password); nav('/'); }catch(err){ alert('Register failed'); }}; return (<div className='container max-w-md mx-auto'><h2 className='text-xl font-semibold my-4'>Register</h2><form onSubmit={handle} className='space-y-3 bg-white p-4 rounded shadow'><input required value={name} onChange={e=>setName(e.target.value)} placeholder='Name' className='w-full border px-3 py-2' /><input required value={email} onChange={e=>setEmail(e.target.value)} placeholder='Email' className='w-full border px-3 py-2' /><input required type='password' value={password} onChange={e=>setPassword(e.target.value)} placeholder='Password' className='w-full border px-3 py-2' /><button className='w-full bg-indigo-600 text-white py-2 rounded'>Register</button></form></div>); }
